{
  "name": "micro-task-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Root scripts for frontend and backend development",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n backend,frontend -c yellow,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "concurrently -n backend,frontend -c yellow,cyan \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "verify": "npm run lint --prefix frontend && npm run typecheck --prefix frontend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
